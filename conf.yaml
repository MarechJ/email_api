host: localhost
port: 8080

# You cam put fake value here but not email will be sent
providers:
  sendgrid:
    user: # add you user
    key: # add you key
  mailgun:
    user: # add user
    key: # add key
